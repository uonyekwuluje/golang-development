name: CI-Builds

on: [push] 

jobs:
  go-script:
    runs-on: 'go'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Go Scripts 
        run: |
           env
           #go run hello.go
        shell: bash {0}
