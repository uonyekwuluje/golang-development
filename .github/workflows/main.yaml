name: CI-Builds

on: [push] 

jobs:
  go-script:
    runs-on: 'go'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Go Scripts 
        shell: bash {0}
        run: |
           go run hello.go
